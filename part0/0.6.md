## New Note In Single Page App Diagram

```mermaid
flowchart TD
    A[User Input Notes and Saves] 
    --> B[Javascript Handles Event]
    --> C[Post Request Sent]
    --> D[Server Saves Data and Returns JSON]
    --> E[Browser Updates Array]
    --> F[DOM Updates Without Refreshing Page]
    --> G[Input Note Renders On Page]
```